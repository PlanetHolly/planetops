{
  "name": "planetops",
  "private": true,
  "description": "Planet Apparel operating system — static surfaces served behind the frontdoor-gate (gate/index.js). This root package.json exists so Railway's Node builder deploys the gate; the site itself is static.",
  "scripts": {
    "start": "node gate/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
